apiVersion: v1
kind: Service
metadata:
  name: weather-backend
spec:
  type: NodePort
  selector:
    app: weather-backend
  ports:
    - protocol: TCP
      port: 5500
      targetPort: 5500
      nodePort: 30550